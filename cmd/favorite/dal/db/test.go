package db

import (
	// "context"
	// "log"
	"testing"
	// "tiktok/dal/model"
)

func TestQueryUser(t *testing.T) {
	// Init()
	// user, err := CreateFavorite(context.Background(), 1, 2)
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// log.Println(user)
}
